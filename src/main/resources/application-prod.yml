server:
  port: 8082
spring:
  datasource:
    url: jdbc:mysql://host.docker.internal:3306/service?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: sqlpas

  liquibase:
    enabled: true
    change-log: classpath:data/db-changelog-master.xml